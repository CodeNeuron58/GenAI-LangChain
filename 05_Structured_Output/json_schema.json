{
  "title": "Student",
  "description": "Schema for student data structure - name is required, age is optional",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Student's full name"
    },
    "age": {
      "type": "integer",
      "description": "Student's age in years"
    }
  },
  "required": ["name"]
}
